
/// after collecting the ip:port and the name, try to connect to the server
/// 
/// if connection is not successful/fail,
/// then print error message and return error
pub fn connect_to(_server_address: &str, _name: &str) -> Result<(), &'static str> {
  // Add logic for connecting to the server here
  // Return Ok(()) if successful, Err("Error message") if failed
  Ok(())
}
